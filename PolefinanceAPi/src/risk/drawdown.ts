export type DrawdownEpisode={peakIndex:number;troughIndex:number;recoveryIndex:number|null;depth:number;duration:number;recoveryDuration:number|null};
export function drawdownEpisodes(equity:number[]):DrawdownEpisode[]{const out:DrawdownEpisode[]=[];if(!equity.length)return out;let peak=0,i=1;while(i<equity.length){if(equity[i]>=equity[peak]){peak=i;i++;continue;}const peakIndex=peak;let trough=i;while(i+1<equity.length&&equity[i+1]<equity[peakIndex]){i++;if(equity[i]<equity[trough])trough=i;}let recovery:number|null=null;for(let j=i+1;j<equity.length;j++){if(equity[j]>=equity[peakIndex]){recovery=j;break;}}out.push({peakIndex,troughIndex:trough,recoveryIndex:recovery,depth:equity[trough]/equity[peakIndex]-1,duration:(recovery??equity.length-1)-peakIndex,recoveryDuration:recovery==null?null:recovery-trough});if(recovery==null)break;peak=recovery;i=recovery+1;}return out;}
export function averageDrawdown(equity:number[]){const e=drawdownEpisodes(equity);return e.length?e.reduce((s,x)=>s+x.depth,0)/e.length:0;}
